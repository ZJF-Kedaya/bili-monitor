const mysql=require('mysql2/promise');
(async()=>{try{const c=await mysql.createConnection({host:'mysql6.sqlpub.com',port:3311,user:'kedaya_zjf',password:'vSma8mlCSt4K0pax',database:'mylogs',connectTimeout:15000});await c.query('CREATE TABLE IF NOT EXISTS bili_monitor_logs (id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY, t BIGINT NOT NULL, level VARCHAR(20) NOT NULL, msg TEXT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4');const r=await c.query('SHOW TABLES');console.log('OK '+JSON.stringify(r[0]));await c.end();}catch(e){console.error('ERR '+e.message);process.exit(1);}})();